import { isRoutingError } from './errors';
import { RouteDefinition, Route, RouteMiddleware, RouteParams, Query } from './types';
import Router6 from './Router6';

export default Router6;

export { isRoutingError, RouteDefinition, Route, RouteMiddleware, RouteParams, Query };
